---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: modelcheckpoints.propeller.absmach.io
spec:
  group: propeller.absmach.io
  names:
    kind: ModelCheckpoint
    listKind: ModelCheckpointList
    plural: modelcheckpoints
    singular: modelcheckpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.sizeBytes
      name: Size
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelCheckpoint is the Schema for the modelcheckpoints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelCheckpointSpec defines the desired state of ModelCheckpoint
            properties:
              algorithm:
                description: Algorithm is the aggregation algorithm used
                type: string
              federatedJobRef:
                description: FederatedJobRef is a reference to the parent FederatedJob
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              metadata:
                description: Metadata contains additional metadata about the checkpoint
                properties:
                  metrics:
                    description: Metrics contains training metrics
                    x-kubernetes-preserve-unknown-fields: true
                  numParticipants:
                    description: NumParticipants is the number of participants
                    type: integer
                  totalSamples:
                    description: TotalSamples is the total number of samples used
                    type: integer
                type: object
              modelRef:
                description: ModelRef is the OCI reference to the model artifact
                type: string
              roundRef:
                description: RoundRef is a reference to the TrainingRound that produced
                  this checkpoint
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - algorithm
            - federatedJobRef
            - modelRef
            type: object
          status:
            description: ModelCheckpointStatus defines the observed state of ModelCheckpoint
            properties:
              phase:
                description: Phase is the current phase of the checkpoint
                type: string
              sizeBytes:
                description: SizeBytes is the size of the checkpoint in bytes
                format: int64
                type: integer
              uploadTime:
                description: UploadTime is when the checkpoint was uploaded
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
